function setup() {
  createCanvas(window.innerWidth, window.innerHeight);
  background(0);
}

function draw() {
  //background(17);
  //ellipse(mouseX, mouseY, 20, 20);
}

function mouseDragged(){
  ellipse(mouseX, mouseY, 20, 20);
}
